import React from 'react';
import PropTypes from 'prop-types';
import { AlertWrapper } from './Alert.styled';

const Alert = () => <AlertWrapper>Alert Component</AlertWrapper>;

Alert.propTypes = {};

Alert.defaultProps = {};

export default Alert;
